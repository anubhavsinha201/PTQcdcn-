# --- Excluded from the public repo deliberately ---

# Unpublished manuscript drafts (co-authored; not for public release yet)
crazystuff.docx
crazystuff.pdf
renooo.docx

# Third-party copyrighted PDFs -- cited by URL in the README instead
Research Papers/

# Personal biometric data: a self-portrait used as an ad-hoc inference test
# ("Your photo (cropped)"). Not dataset imagery -- keep it out of a public repo.
cdcn_figures/face_test_result.png

# Broken/empty virtualenv (created under WSL, no packages, wrong layout for Windows)
cdcn_gpu_env/

# Local tooling state
.claude/

# --- Standard Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# --- Training / conversion artifacts too large or machine-specific to track ---
# The trained Keras checkpoint is not distributed; the .tflite files in
# cdcn_quantized/ are the released model artifacts.
*.weights.h5
*.keras
cdcn_quantized/saved_model/

# Downloaded on first run into ~/cdcn_assets by the notebook
haarcascade_frontalface_default.xml
lbfmodel.yaml

# --- OS / editor noise ---
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
